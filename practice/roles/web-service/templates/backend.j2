server {
      listen 80;
      server_name ansible.mfilipovich.playpit.net;
      location /msg-service {
          proxy_pass http://{{ backends.0.hostip }}:{{ backends.0.hostport }}/;
      }
      location /mysql-check {
          proxy_pass http://{{ backends.1.hostip }}:{{ backends.1.hostport }}/;
      }
   }
